-- ---------------------------------------------------------------------------------------
-- Table: norpac_commons.user_phone. Generated by Pareto Factory™ "Be Consistent"
-- ---------------------------------------------------------------------------------------
DROP TABLE IF EXISTS norpac_commons.user_phone CASCADE;

CREATE TABLE norpac_commons.user_phone (
  id                               UUID             NOT NULL    DEFAULT GEN_RANDOM_UUID(), 
  id_user                          UUID             NOT NULL, 
  id_rt_phone_type                 UUID             NOT NULL, 
  number                           VARCHAR(16)      NOT NULL, 
  extension                        VARCHAR(10)      NULL, 
  is_primary                       BOOLEAN          NOT NULL, 
  created_at                       TIMESTAMP        NOT NULL    DEFAULT CURRENT_TIMESTAMP, 
  created_by                       VARCHAR(32)      NOT NULL, 
  updated_at                       TIMESTAMP        NOT NULL    DEFAULT CURRENT_TIMESTAMP, 
  updated_by                       VARCHAR(32)      NOT NULL, 
  is_active                        BOOLEAN          NOT NULL    DEFAULT TRUE
);

ALTER TABLE norpac_commons.user_phone ADD PRIMARY KEY (id);

CREATE UNIQUE INDEX user_phone_alt_key
    ON norpac_commons.user_phone(id_user, LOWER(number), LOWER(extension));

ALTER TABLE norpac_commons.user_phone
  ADD CONSTRAINT user_phone_id_user
  FOREIGN KEY (id_user)
  REFERENCES norpac_commons.user(id)
  ON DELETE CASCADE;
    
ALTER TABLE norpac_commons.user_phone
  ADD CONSTRAINT user_phone_id_rt_phone_type
  FOREIGN KEY (id_rt_phone_type)
  REFERENCES norpac_commons.rt_values(id)
  ON DELETE CASCADE;

CREATE TRIGGER update_at
  BEFORE UPDATE ON norpac_commons.user_phone 
    FOR EACH ROW
      EXECUTE FUNCTION update_at();

